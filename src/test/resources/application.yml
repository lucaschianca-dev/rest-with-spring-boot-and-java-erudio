
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
springdoc:
  pathsToMatch: /api/v2/person/**, /api/v2/book/**
  swagger-ui:
    use-root-path: true
server:
  port: 8888